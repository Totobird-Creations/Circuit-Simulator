[gd_scene load_steps=5 format=2]

[ext_resource path="res://main/world/structure/port.gd" type="Script" id=1]
[ext_resource path="res://assets/texture/strutcure/port.png" type="Texture" id=2]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;



uniform bool hovered = false;



void fragment() {
	vec4 original = texture(TEXTURE, UV);
	vec3 a        = vec3(0.0);
	vec3 ba       = vec3(1.0);
	vec3 bb       = vec3(1.0, 1.0, 0.0);
	vec3 b        = ba + (bb - ba) * float(hovered);
	vec3 colour   = b + (a - b) * original.r;
	COLOR         = vec4(colour, original.a);
}
"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/hovered = false

[node name="port" type="Area2D"]
script = ExtResource( 1 )

[node name="sprite" type="Sprite" parent="."]
material = SubResource( 2 )
scale = Vector2( 0.125, 0.125 )
texture = ExtResource( 2 )
